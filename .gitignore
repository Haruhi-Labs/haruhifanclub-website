# === Node / 前端 ===
node_modules/
dist/
.vite/
*.local
.eslintcache

# === Rust / 后端 ===
target/
**/*.rs.bk

# === 运行时数据（永不进 git，单独备份）===
data/
uploads/
_legacy_dumps/

# === 环境变量（含密钥）===
.env
.env.*
!.env.example
!.env.*.example

# === 编辑器 / 系统 ===
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# === Claude Code 本地工作目录（工作流脚本 / 会话缓存，按需自行版本化）===
.claude/

# === sqlx 离线缓存保留（不要忽略）===
!.sqlx/

# Docker 交叉编译输出（linux 二进制）
target-linux/
